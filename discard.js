// Extract data from iex
// const mappedData = Object.values(
//   data.map((d) => {
//     return d.open !== null
//       ? [Date.parse(d.date), d.open, d.high, d.low, d.close]
//       : undefined;
//   })
// );
